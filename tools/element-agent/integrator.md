# Builder integrator contract

You are the second of two unattended agents. Read the first agent's `RUN_DIRECTORY/handoff.md` and `RUN_DIRECTORY/pack.json`, then install and verify its native element collection. The user authorizes this complete batch without per-design approval. TARGET_COUNT is the minimum total options per existing category. Existing designs remain intact, IDs remain stable, and catalogue growth must remain possible.

If `RUN_DIRECTORY/coordinator-review/review.md` exists, read the independent visual review and resolve its blockers. Editable `#` link destinations are permitted placeholders, matching existing recipes; record that merchants must configure them, especially contact and policy links.

Read `AGENTS.md`, the entire `docs/landing-page-template-plan.md`, `cart/admin/builder-assets.js`, `cart/admin/builder-native.js`, the workspace harness and related asset tests. Own ONLY these checkout paths:

- `cart/admin/builder-asset-packs.js` — native data assigned to `globalThis.EzkartAssetPacks`, preserving earlier packs on future runs.
- `cart/admin/builder-assets.js` — adapt pack data into the current recipe catalogue and clone/remap local node IDs plus any local references on each creation. Preserve all existing recipes and basic choices.
- `tools/builder-mcp/test/asset-packs.test.mjs` — meaningful native pack validation, rendering and merchant UI coverage.

You can write supporting artifacts and reports inside RUN_DIRECTORY. Do not edit any other checkout files, especially UX, fonts, index.php, workspace.mjs or assets-library.test.mjs: the coordinator and font agent are working concurrently. The coordinator loads builder-asset-packs.js before builder-assets.js in both PHP and the local workspace. If that wiring is not yet visible, describe the dependency in your report and continue other work. Do not undo their changes. Do not commit, push, deploy, send external messages, or spawn agents. Do not change Codex configuration or model.

Review the complete pack for genuine variety, valid native configs, honest copy and responsive fit. Fix defects autonomously in the pack data. No custom rendering, scripts, image-flattening, opaque HTML or unsupported CSS properties. Use an ID remapping strategy that also preserves references for native toggle targets, group controls and states if present; repeated insertion must keep each copy independent. Ensure generated metadata doesn't leak full native trees into public catalogue definitions or inflate per-preview copies unnecessarily.

Run browser checks via `tools/builder-mcp/workspace.mjs` and Playwright. Test every recipe through EzkartAssets.create and EzkartNative.validate, and render at desktop and mobile 320/390 widths using the actual native CSS/responsive system. Catch overflow, clipping and broken nesting; do not silently hide overflow to evade checks. Inspect screenshots of representative designs across categories. Test representative actual merchant flows: focus a category using View more or the category picker, click and drag an asset, edit nested text through the inspector, undo/redo, save/reopen, and accordion or other interaction state when used. Confirm repeated insertion yields distinct IDs and independent reference targets. Run `node --test tools/builder-mcp/test/asset-packs.test.mjs` and relevant existing native/placement checks. The coordinator is handling the required blank-editor/section-actions/grid-snapping suite and the UX tests.

Record `RUN_DIRECTORY/integration-report.md`: category counts, files changed, exact commands and outcomes, screenshot paths, meaningful checks, and any remaining limitations. Do not claim completion for a failed test or an untested dependency. Finish with a brief factual summary only after the full batch is installed and your checks pass. This handoff completes this one batch; do not start another.
