# Customer order tracking

The payment page links to `/cart/return.php?order=…&shop=…` after verified payment. Existing hosted-payment return links continue to use the same page. Customers can bookmark the order link and follow payment, seller acceptance/preparation, pickup arrangement, actual courier collection, transit, delivery, and exceptions.

Booking a shipment or receiving a waybill never marks it as picked up. A sandbox payment that skipped shipping shows a completed test payment and no shipping progress or map. Tracking updates automatically, without a manual refresh button. The progress indicator uses the original navy palette and the status heading has no check icon. Payment polling stays local and no longer toggles the button's disabled state; manual checks have an accessible busy state and can join an existing background request.

## Customer Google sign-in

Tracking pages and the sandbox walkthrough require Google login through the existing Supabase project. Signed-out visitors stay on their tracking URL and see a compact “Sign in to track” dialog over an empty loading shell. One click opens the Google account chooser in a popup; the original tab loads the order only after its server session is authenticated. Browsers that block the popup use the same Google flow in the current tab and return to the preserved order/stage link. The old standalone login page redirects back to tracking. `/cart/login.php` starts a CSRF-protected S256 PKCE flow; `/cart/admin/customer-auth.php` reuses the existing `/cart/admin/**` callback allowlist. It has a separate `ezkart_customer` HTTP-only, SameSite=Lax cookie scoped to `/cart`, with private session storage configured by `customer_session_storage`. It never grants merchant/admin access. Supabase `/user` verifies the Google identity and confirmed email. Existing verified TOTP factors must be completed before access; tokens refresh server-side and never enter page HTML or browser-readable storage.

Before showing the dialog, a CSRF-protected request to `/cart/admin/customer-session.php` checks for an existing Ezkart Google login on this host. This path receives the existing admin-scoped cookie and reuses its session validation and refresh under the admin lock, then verifies the identity and MFA through Supabase. Only the verified user ID/email and a short-lived expiry (at most ten minutes, capped by the original token expiry and absolute sign-in lifetime) are mirrored into the customer session. No merchant permissions or provider tokens are copied. The bridge rechecks automatically when needed; explicit customer sign-out disables automatic reuse, and a password-only admin session cannot authenticate a customer.

Popup completion is detected through `/cart/api/customer-session.php`, not browser messages or a popup-supplied identity. This works even when the provider severs the opener with COOP. The callback still uses a one-use PKCE verifier; MFA stays in the popup. A late anonymous poll cannot overwrite the cookie regenerated by successful login.

Signed-in checkout records the immutable Supabase user ID on the order. Guest checkout remains available; the first tracking visit can claim an order only when the verified account email matches the checkout email. The server persists that immutable user ID under the order lock. Subsequent access follows the ID, so another account cannot claim an already-linked order through an email match. Wrong-account and nonexistent references return the same 404 response. Unsigned tracking requests return 401 before reading an order or contacting Biteship. Page login preserves the validated order/shop link and prevents external redirect targets.

## Provider data

Tracking requests use `GET /cart/api/status.php?order=…&tracking=1`. The server calls Biteship's [Retrieve an Order](https://biteship.com/en/docs/api/orders/retrieve) endpoint, which includes courier history, tracking link and nullable origin/destination coordinates. Credentials are selected from the stored order environment. It never creates a shipment or changes payment status.

A two-minute cache shared by all viewers limits provider calls, including failed requests. Network calls happen outside the order lock. A webhook arriving while the provider read is in progress takes precedence over that response. Provider outages preserve the last confirmed status and display a retry notice. Authenticated `order.status` callbacks update the local timeline between provider reads; price and waybill callbacks cannot reset shipment status. Snake-case and camel-case [Biteship status values](https://biteship.com/en/docs/api/trackings/status) are supported. Duplicate and delayed notifications cannot reverse completed delivery/return milestones.

The authenticated customer projection includes delivery status, bounded timestamped history, courier/service, waybill, HTTPS tracking link, and validated delivery coordinates. Provider keys, full provider responses, addresses and driver contact details are not added to the public API. The public payment poll omits all shipment fields. Shipment access requires both the order reference and an authenticated customer who owns the order. External links omit referrers; map tiles send only the site origin, never the order query.

## Google Maps and route display

Tracking now uses the Google Maps JavaScript API, AdvancedMarkerElement, and the Routes library. Leaflet and OpenStreetMap are no longer loaded. The embedded map keeps Google's attribution and uses a light map with Ezkart's own truck, delivery marker, route line, and accessible controls. The status progress remains navy, without a header icon or manual refresh button.

A validated courier scan or confirmed pickup/delivery supplies the package position. The truck is centered on that last reported position, with the scan timestamp above the map. It is not an animated or interpolated live vehicle. Delivery and confirmed return replace the truck with a completed destination marker. Without a reported position, the map can show known pickup/delivery points; it never places a truck on a guessed location. Returns use the seller as the route destination.

Google's `Route.computeRoutes` supplies a road path from the last reported position (or pickup if no scan is available) to the destination. The line is labeled **Suggested road route** because it is not Biteship's actual driven path. No arrival estimate or inferred shipment progress is calculated from that route. The page requests only `path`, caches paths in memory for unchanged coordinates, ignores stale asynchronous results, and clears the line at terminal states. A route failure keeps known markers and the timeline available; there is no fabricated straight-line fallback.

The default view focuses on the package. **View full route** fits the route and known endpoints. Zoom, mouse-wheel/touch gestures, dragging, and recentering remain available. Automatic status updates preserve the customer's zoom/pan. The Google SDK loads only after authentication, order authorization, available coordinates, and the map entering the viewport. A missing/blocked SDK or rejected key leaves the confirmed updates and Google Maps coordinate link available. Provider text is never inserted as HTML.

For instant/same-day services, an HTTPS courier tracking link is shown separately when Biteship supplies one. This does not imply an embeddable GPS feed. See [Biteship's instant courier tracking guide](https://help.biteship.com/hc/en-us/articles/58381493064985-How-to-Track-Instant-Couriers-in-Real-Time).

### Test-site setup

Google sign-in and Google Maps use separate credentials. For the workbench, Google's [Maps Demo Key](https://developers.google.com/maps/documentation/javascript/demo-key) supports prototype maps, markers and Compute Routes without adding billing details, subject to Google's prototype quotas and terms. It is not for production.

Put the key in the test site's ignored `config.runtime.php`, preserving the existing Auth/Worker/payment settings:

```php
'google_maps_browser_key' => 'YOUR_TEST_BROWSER_KEY',
'google_maps_map_id' => 'DEMO_MAP_ID',
```

Equivalent environment variables are `EZKART_GOOGLE_MAPS_BROWSER_KEY` and `EZKART_GOOGLE_MAPS_MAP_ID`. The test deployment defaults to Google's demo map ID; production requires its own configured JavaScript map ID. A map ID is not an API key. A custom cloud map style can later be attached to the map ID without changing the tracking code.

For a standard project, enable **Maps JavaScript API** and **Routes API** and use a browser key restricted to the test website (`https://test.ezkart.id/*`). The browser key is necessarily visible in SDK requests; never use a backend credential or commit it. Production requires a separate setup and the existing release approval. Google's [setup documentation](https://developers.google.com/maps/documentation/javascript/get-api-key) covers standard keys and billing.

## Verification (21 September 2026)

`PHP_BINARY=php node --test tools/checkout-test/checkout.test.mjs`

The provider transport uses fixtures and cannot contact payment or shipping providers. Coverage includes the existing checkout/payment/merchant flows plus seller-to-courier progression, provider throttling and outages, wrong-order payloads, invalid links/coordinates, webhook retries/status aliases/returns, and an in-flight automatic payment check joined by a manual click. Browser checks cover 1280px and 390px widths, a centered package marker, visible zoom controls, Google gesture configuration, preserved zoom/pan during automatic polling, optional route view and recentering, absent coordinates, safe timeline/tooltips, delivery confirmation, request errors and horizontal overflow. Authentication checks cover CSRF, PKCE, one-use callbacks, verified Google identities, ownership and guest claims, immutable account binding, MFA, refresh failures, logout, Google popup completion/cancellation, blocked-popup fallback, existing-login reuse and its password/MFA/CSRF restrictions, and shipment redaction from the public payment poll. The Google SDK/Routes contract is stubbed in automated tests so no provider requests or billing occur. Additional checks cover route reuse, stale route results, return destinations, terminal route removal, missing keys, SDK network/auth failures, and routing failures. Actual Google rendering, gestures, key restrictions and visual acceptance require an authorized key and a browser run; the contract fixture does not establish those results.

These checks do not establish live courier GPS availability or production provider acceptance. Work remains on `agent/ezkart-workbench`; the existing production release hold applies.

## Interactive sandbox walkthrough

Open `/cart/tracking-sandbox.php?stage=transit` on `test.ezkart.id` and sign in with Google. It renders the same customer page using sample orders projected through `ez_public_order_tracking`, without creating orders, modifying cart storage, calling payment/shipping providers, or writing payment/shipment state. The page is available only when deployment is `test` and commerce mode is `sandbox`; production returns 404.

Choose a stage, advance one step, or run the eight-stage walkthrough at five-second intervals. It pauses when the tab is hidden and stops at delivery. Additional scenarios cover delays, cancellation, returns, provider unavailability, and absent coordinates. A `stage` query parameter preserves the selected view for sharing. The persistent banner identifies all order/map data as simulated. This is a UI walkthrough, not evidence of a completed DOKU/Biteship sandbox transaction.
